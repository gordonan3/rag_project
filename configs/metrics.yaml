input_file: "results/rag_full.csv"

ground_truth_column: "ground_truth"

prediction_columns:
  - "rag_qwen"
  - "rag_ministral"

metrics:
  rouge: true
  bleu: true
  comet: true

comet:
  model_name: "Unbabel/wmt22-comet-da"
  batch_size: 8
  gpus: 1
